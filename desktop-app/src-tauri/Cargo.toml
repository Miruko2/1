[package]
name = "hanako-editor"
version = "1.0.0"
description = "HANAKO local project editor"
authors = ["HANAKO"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.5", features = [] }

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
tauri = { version = "2.11", features = [] }
