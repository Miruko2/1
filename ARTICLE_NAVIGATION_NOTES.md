# 文章导航整合说明

已将 `hanako-liquid-background-design` 中截图对应的文章导航功能迁入主目录的日记页面。主站仍从 `index.html` 启动，继续读取原有 `projects.js`，无需安装 React 或执行构建。

| 对比项 | 原项目 | hanako-liquid-background-design | 此次处理 |
| --- | --- | --- | --- |
| 页面结构 | 原生 HTML、CSS、JavaScript；交互主要位于 `index.html` | React、TypeScript、Vite；拆为 `App.tsx`、`hanako.ts`、`hanako.css` | 保留原生结构，新增独立的文章导航脚本与样式 |
| 文章内容 | `projects.js` 中的 5 篇现有文章，本地封面，配套 `admin.html` 编辑器 | `src/data/projects.ts` 中的 8 张演示卡片，使用演示文章和外链封面 | 保留全部原文、图片路径与编辑器 |
| 文章目录 | 无 | 开头目录卡、侧边章节轨道、平滑跳转、标题与花瓣动效、阅读高亮 | 全部接入原项目的正文面板 |
| 标题样式 | 原先主要设置 h3、h4 | 补充 h1、h2 样式及目录相关样式 | 补齐标题样式，导航识别 h1 至 h6 |
| 液态背景 | 背景 iframe 加 `simple-liquid-bg.js` | 背景 iframe 加页面渐变兜底 | 保留现有背景效果；修复旧初始化脚本的作用域错误 |
| 无封面的文章 | 仍可使用独立的正文面板 | 某些内容改为写入克隆卡片 | 保留原项目的正文显示逻辑 |
| GSAP | 页面通过 CDN 使用 3.12.7 | package.json 声明 ^3.15.0 | 复用现有版本，无新增依赖 |

`liquid-refraction-lab` 是独立的 Next.js / Three.js 液态折射实验项目，负责背景视觉，不包含日记文章目录。此次没有改动该目录，也没有修改设计参考目录。

## 已接入的效果

- **开头目录卡**：从正文非空标题自动生成编号与层级缩进；条目由左侧带模糊淡入，悬停时右移并显示箭头。
- **右侧章节轨道**：包含“目录”返回节点和各章节节点；填充线在相邻章节之间连续推进。宽屏放在面板外，右侧空间不足或屏幕宽度不超过 860px 时移入面板。悬停或键盘聚焦轨道时展开标签。
- **章节跳转**：复用 GSAP，以 `power3.inOut` 平滑滚动，距离越远耗时越长；到达后显示标题下划线、柔光和迷你花瓣。网络图片在跳转中改变排版时，会重新校准终点。
- **同步高亮**：正文滚动、点击目录或点击侧边节点时，目录与轨道同步标记当前章节。完整显示的标题保持清晰，避免被正文边缘模糊遮住。

目录在文章包含至少 **2 个非空 HTML 标题**时显示，例如 `<h2>章节</h2>`、`<h3>小节</h3>`。纯文本文章保持原样。后续编辑文章时不需要手动维护目录。

同时补充了按钮键盘操作、当前章节的辅助技术标记、减少动态效果偏好，以及关闭面板时对滚动动画、事件监听、布局观察器和花瓣的清理。原页面手机竖屏的旋转提示继续保留，手机阅读按原有横屏方式使用。

## 文件变更

| 文件 | 作用 |
| --- | --- |
| `index.html` | 引入导航资源，将创建、刷新与销毁接入文章面板；协调标题清晰度与正文渐隐 |
| `article-navigation.js` | 目录生成、侧边轨道、滚动进度、平滑跳转、花瓣、键盘操作与资源清理 |
| `article-navigation.css` | 目录、轨道、标题动效、窄屏适配及减少动态效果样式 |
| `simple-liquid-bg.js` | 改为在同一异步函数内加载背景模块，使成功和失败正确传回调用方；保留视觉参数 |

## 验证记录

已在本地浏览器检查 1680×960、1280×720 与 844×390 三种尺寸，覆盖外置和内置轨道；使用现有的六章节文章、两章节文章和无标题文章验证目录生成。

已核对逐章跳转的落点与双向高亮、连续阅读进度、跳转后继续滚动、键盘末节点跳转、长文快速跳转时的图片加载影响、关闭后的导航与花瓣清理。脚本及 CSS 语法检查通过；背景初始化已输出成功日志，未再出现原先的 `reject is not defined` 报错。

`projects.js` 与 `admin.html` 的文件校验值保持不变。
